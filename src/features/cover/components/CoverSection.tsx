import {
  GROOM_NAME_SHORT,
  BRIDE_NAME_SHORT,
  WEDDING_DATE_DISPLAY,
  VENUE_NAME,
} from "@/lib/constants";

interface CoverSectionProps {
  onOpen: () => void;
}

export function CoverSection({ onOpen }: CoverSectionProps) {
  return (
    <section
      id="cover"
      // className="min-h-screen flex flex-col items-center justify-center relative bg-[#fdf6ee] px-8 py-16 overflow-hidden"
    >
      
    </section>
  );
}
